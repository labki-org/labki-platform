# Labki Configuration Secrets
# Copy this file to 'secrets.env' and customize it.

# ------------------------------
# Site Identity (First Run Only)
# ------------------------------
# These settings are used only when installing the wiki for the first time.
# Changing them later requires editing LocalSettings.user.php or database values.
MW_SITE_NAME=Labki
MW_SITE_LANG=en

# The full URL of your wiki
MW_SERVER=http://localhost:8080

# ------------------------------
# Admin Account (First Run Only)
# ------------------------------
MW_ADMIN_USER=admin
# CHANGE THIS! The initial password for the admin account.
MW_ADMIN_PASS=change-me

# ------------------------------
# Database Configuration
# ------------------------------
# Default settings for the bundled database.
# If connecting to an external DB, update host/user/pass accordingly.
MW_DB_HOST=db
MW_DB_NAME=labki
MW_DB_USER=labki
MW_DB_PASSWORD=labki_pass
MARIADB_ROOT_PASSWORD=root_pass

# ------------------------------
# Development Options
# ------------------------------
# Set to 1 to disable the platform's bundled extensions (SMW, etc.)
# This is useful for testing in a "clean" environment.
MW_DISABLE_PLATFORM_EXTENSIONS=0
